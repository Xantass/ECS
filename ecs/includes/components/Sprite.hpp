#pragma once

#include "raylib.h"
#include "Frame.hpp"

struct Sprite {
    Texture2D texture;
    Frame frame;
    // std::string currentAnimation;
    // float animationTimer = 0.0f;
    // int animationFrameIndex = 0;
};
